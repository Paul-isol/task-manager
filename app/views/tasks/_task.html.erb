<div id="<%= dom_id task %>" class="w-full sm:w-auto my-5 space-y-5 flex flex-col">
    <h2 class="text-2xl font-semibold"><%= task.title %></h2>
    <p class="text-gray-600"><%= task.description %></p>
    <p class="text-sm text-gray-500">Status: <%= task.status %></p>
</div>
